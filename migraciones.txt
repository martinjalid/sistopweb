CREATE TABLE IF NOT EXISTS `perfil` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `nombre` varchar(15),
  PRIMARY KEY (`id`))
ENGINE = InnoDB;

INSERT INTO perfil(nombre) VALUES('Administrador');

ALTER TABLE administrador ADD COLUMN activo TINYINT NOT NULL DEFAULT 1;
